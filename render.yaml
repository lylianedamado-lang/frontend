services:
  - type: static_site
    name: data-clean-frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist/data-clean-frontend/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
